import { JSX } from 'react';
import { Card }  from "@mui/material";
import { ITask } from "@/types/tasks";

export const  Task = (taskProps: ITask):JSX.Element => {
    return (
      <Card>

      </Card>
    )
}