/**
 * functions to manage the behavior of lists, or task groupings
 */
import {ITask} from "@/types/tasks";

/**
 * create new task list
 */
export const createTaskList = () => {

}

/**
 * Viewing and switching between task lists
 */
export const selectTaskList = () => {

}

/**
 *  Adding new task
 */
export const addNewTask = (task: ITask) => {

}

/**
 * delete a task
 */
export const removeTask = (id: ITask['id']) => {
}